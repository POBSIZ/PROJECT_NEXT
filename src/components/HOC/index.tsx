import CustomSuspense from './customSuspense';
import AosWrapper from './Aos';
import Chart from './chart';
import BlockChangePage from './blockChangePage';

const Hoc = { CustomSuspense, AosWrapper, Chart, BlockChangePage };

export default Hoc;
export { CustomSuspense, AosWrapper, Chart, BlockChangePage };
