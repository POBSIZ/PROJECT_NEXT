import CustomSuspense from './customSuspense';
import AosWrapper from './Aos';
import Chart from './chart';

const Hoc = { CustomSuspense, AosWrapper, Chart };

export default Hoc;
export { CustomSuspense, AosWrapper, Chart };
