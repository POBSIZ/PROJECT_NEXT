import styled, { css } from 'styled-components';

//import { MoveParamsType } from '.';

const StyledProgressBar = styled.header.attrs((props) => ({}))`
  ${(props) => {
    return css``;
  }};
`;

export default StyledProgressBar;
