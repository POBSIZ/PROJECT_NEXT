import styled, { css } from 'styled-components';

const StyledArticleList = styled.header.attrs((props) => ({}))`
    ${(props) => {
        return css`
        `
    }};
`;

export default StyledArticleList;